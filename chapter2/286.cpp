#include <stdio.h>

int main() {
	int bias = 2^(15 - 1) - 1;

	// 1. 最小正非规格化数

	// 二进制: 0  0...0(15) 0 0...(62)1
	// 十进制: 2 ^ (1 - bias - 63)

	// 2. 最小正规格化数

	// 二进制: 0  0...(14)1 1 0...0(63)
	// 十进制: 1 - 2^(1 - bias)

	// 3. 最大的规格化数

	// 二进制: 0  1...(14)0 1 1...1(63)
	// 十进制: 2 ^ bias * (2 - 2^(-63))
	// 2 - 2^(-63) 为等比数列的求和
	// => 推导: 1.1(63) << 63 = 1(64) * 2^(-63)
	// 等比数列求和:  1(64) = 2^0 + 2^1 + 2^2 + ... + 2^63 + 2^64 = 2^64 - 1 
}